@model _5051.Models.AdminProfileModel
@{
    ViewBag.Title = "Delete";
}
<ol class="breadcrumb">
    <li><a href="~/Admin/MainHome">Home</a></li>
    <li><a href="~/Admin/StudentHome">Students</a></li>
    <li class="active">Delete</li>
</ol>


<div class="panel panel-primary">
    <div class="panel-heading">
        <h3 class="panel-title"></h3>
    </div>
    <div class="panel-body">
     

            <div class="container">
                <div class="row">


                    <div class="container">
                        <div class="row">

                            @{
                                if (Model == null)
                                {
                                    <p>No Avatar</p>
                                }

                                else
                                {
                                    <div class="row">
                                        
                                        <br />
                                        <br />

                                        <div class="row">
                                            <div class="col-sm-5 text-right">
                                            </div>
                                            <div class="col-sm-5">
                                                <img src="~/Content/img/@Model.Uri" width="70" height="120" />
                                                <br />
                                                <span>
                                                    <strong>
                                                        <text style="text-align:center">
                                                            @Model.Name
                                                        </text>
                                                    </strong>
                                                    <br />
                                                    <br />
                                                    <br />
                                                </span>
                                            </div>
                                        </div>

                                        @using (Html.BeginForm())
                                        {
                                            @Html.AntiForgeryToken()
                                            @Html.HiddenFor(m => m.Id)
                                            @Html.HiddenFor(m => m.Uri)
                                            @Html.HiddenFor(m => m.Name)
                                            @Html.HiddenFor(m => m.PowerId)
                                            @Html.HiddenFor(m => m.PersonalContact)
                                            @Html.HiddenFor(m => m.GuardianContact)
                                            @Html.HiddenFor(m => m.Address)

                                            <div class="row">
                                                <div class="col-sm-5 text-right">
                                                </div>
                                                <div class="col-sm-4">
                                                    <input type="submit" value="Delete" class="btn btn-danger">
                                                    @Html.ActionLink("Cancel", "StudentHome", new { id = Model.Id }, new { @class = "btn btn-success" })

                                                </div>
                                            </div>
                                        }

                                    </div>
                                }

                            }
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>


